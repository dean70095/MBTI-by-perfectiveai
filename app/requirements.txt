# Streamlit Cloud picks this up because it sits next to the entry-point script.
# Slim production deps — excludes training-time stack (torch, transformers,
# sentence-transformers, nltk, matplotlib, seaborn, rank_bm25) which are not
# needed at inference. Keeping this list small avoids OOM on the free tier.
streamlit>=1.30,<2
numpy>=1.24,<3
pandas>=2.0,<3
scikit-learn>=1.7,<1.9
